import { ScrollBotDirective } from './scroll-bot.directive';

describe('ScrollBotDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollBotDirective();
    expect(directive).toBeTruthy();
  });
});
